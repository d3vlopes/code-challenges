pre-commit:
  commands:
    check:
      run: npm run check -- --files-ignore-unknown=true {staged_files} && git update-index --again
    test:
      glob: '*.{ts, tsx}'
      run: npm run test

pre-push:
  commands:
    test:
      run: npm run test:ci
    # packages-audit:
    #   run: npm audit
